[project]
name = "lilalearnbackend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "pydantic>=2.12.5",
    "requests>=2.32.5",
]

[tool.uv.workspace]
members = [
    "lilalearn",
]
